public class Dias extends Godsj {

	Ship2 ship = new Ship2();
	
	public Ship2 Strike(Ship2 ship.getName , Ship2 ship.getTolerance ) {
		if (ship.getTolerance() > 0 && ship.getName == "a" || ship.getName == "b") {
			ship.setTolerance() == 0;
		}else {
			ship.setTolerance() == 1;
		}
        return ship.getTolerance();	
	}

	public Ship2 Resurrection(Ship2 ship.getTolerance) {
		if (ship.getTolerance() > 0) {
			ship.getTolerance() = 3;
			return ship.getTolerance();
		}
	}
}
